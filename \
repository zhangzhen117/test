#include "ZHEN.h"
int main(){
    vector<int> vec={1,2,3,4,5,6,7,8,9,0};
    list<int> l(5);
    copy(vec.begin()+2,vec.begin()+7,l.rbegin());
    for(auto s:l)
        cout<<s<<"\n";
    return 0;
}
